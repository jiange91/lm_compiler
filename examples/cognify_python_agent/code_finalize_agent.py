system_prompt = """
You are a code expert. Given an incomplete function and the function body generated by another agent, your task is to evaluate and improve the function body, ensuring it meets the criteria.
"""

import cognify
from cognify.hub.cogs.reasoning import ZeroShotCoT

lm_config = cognify.LMConfig(
  custom_llm_provider="openai",
  model="gpt-4o-mini",
  kwargs={"temperature": 0.0},
)

code_finalize_agent = cognify.Model(agent_name='code finalize', system_prompt=system_prompt,
                            input_variables=[cognify.Input(name='incomplete_function'), cognify.Input(name='completed_code')],
                            output=cognify.OutputLabel(name='finalized_code', custom_output_format_instructions='Please response with the function body only. Place the code within <result> and </result> tags. Do not include any additional text, explanations, or comments outside these tags.'),
                            lm_config=lm_config)